Your next job is to document the yaml format for monsters.  You will create a file MONSTERS.md.  Draw information from these sources:

  - monster-project.md.  Some of these are out of date.
  - YAML.md for the player keys.
  - ~/bin/monster-json-to-yaml, which should be trusted to be up to date.
  - ~/bin/angrycalc and ~/bin/angry2tex, which should be trusted to be up to date.
  - ~/etc/dnd/my-adventures/monsters/*.yaml, which should be trusted to be up to date.

You will have questions, especially where you may spot an inconsistency. What are those questions?
